name: tool_base_network
description: tool base network package.
publish_to: none

environment:
  sdk: ^3.9.0
  flutter: "^3.32.8"

dependencies:
  flutter:
    sdk: flutter

  core_network:
    path: ../../../core/network
